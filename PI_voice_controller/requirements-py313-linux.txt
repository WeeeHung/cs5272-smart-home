# Python 3.13 on Linux (e.g. Raspberry Pi): two separate issues block a plain "pip install openwakeword":
#   - PyPI 0.6.0 requires tflite-runtime (no cp313 wheels).
#   - GitHub main requires speexdsp-ns on Linux (no cp313 wheels as of early 2026).
# This voice app does not use Speex noise suppression (openWakeWord default), so skipping speexdsp-ns is fine.
#
# Run from repo root (after git pull so this file exists), in order:
#
#   pip install 'openwakeword @ git+https://github.com/dscripka/openWakeWord.git' --no-deps
#   pip install -r PI_voice_controller/requirements-py313-linux.txt
#
onnxruntime>=1.10.0,<2
ai-edge-litert>=2.0.2,<3
tqdm>=4.0,<5.0
# scipy: resample USB mic when hardware only offers 48k/44.1k (also pulled by openwakeword)
scipy>=1.3,<2
scikit-learn>=1,<2
requests>=2.0,<3
pyaudio
numpy
