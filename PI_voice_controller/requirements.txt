# Voice controller deps. Use >=0.6 so Model(wakeword_models=..., inference_framework=...) matches
# current API; older wheels (e.g. on Python 3.13) forward bad kwargs to AudioFeatures and crash.
openwakeword>=0.6.0
pyaudio
numpy
